# 券码分发系统 (Gift System) - 环境变量配置

## 必需配置

### Cloudflare D1 数据库
```
DB=your_d1_database_binding
```

### JSONBin.io 配置
```
JSONBIN_API_KEY=your_jsonbin_master_key
```

### EmailJS 邮件服务
```
EMAILJS_SERVICE_ID=your_service_id
EMAILJS_TEMPLATE_ID=your_template_id
EMAILJS_PUBLIC_KEY=your_public_key
EMAILJS_PRIVATE_KEY=your_private_key
```

### 管理员 Token（可选，默认为 ai2024）
```
ADMIN_TOKEN=your_secure_random_token
```

## 可选配置

### 调试模式
```
DEBUG=false
```

## 管理员访问方式

管理后台通过 URL 参数访问：
```
?admin=YOUR_ADMIN_TOKEN
```

示例：
```
https://your-domain.com/?admin=ai2024
```

## 部署说明

1. 复制此文件为 `.env` 并填写实际值
2. 在 Cloudflare Pages 设置中配置环境变量
3. 确保 D1 数据库表结构已创建：

```sql
CREATE TABLE IF NOT EXISTS gift_codes (
    email TEXT PRIMARY KEY,
    code TEXT NOT NULL,
    expires_at INTEGER NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```
